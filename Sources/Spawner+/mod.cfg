[mod]

api_version="1.0"
name="Spawner+"
version="2.0.1"
author="rekitrelt"
description="allows toggling spawners (vermin vigil blocks) with an empty hand

also allows left clicking on a spawner with a spawner to increase rate

new_valid_logic doesn't fail to find a spawn location as much as vanilla does (same spawnrate either way but functionally much more possible)"
game_version="4.0.1"
load_order=0
config_editable = true

[dependencies]

[config]
new_valid_logic = true
